// The first is inclusive the second is inclusive 
const char *TERMINAL_BUFFER_START = (char *)0x000;
const char *TERMINAL_BUFFER_END = (char *)0x010; // This allows 16 characters with the last one potetnially being end character